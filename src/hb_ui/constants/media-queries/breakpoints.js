// const tw = require('twin.macro')
// const { css, theme } = tw

const sm = 640
const md = 768
const lg = 1024
const xl = 1280

module.exports = {
  sm: sm,
  md: md,
  lg: lg,
  xl: xl,
  px: {
    sm: `${sm}px`,
    md: `${md}px`,
    lg: `${lg}px`,
    xl: `${xl}px`,
  }
}